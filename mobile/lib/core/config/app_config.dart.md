# app_config.dart

## Description
Application configuration class providing environment variables, backend origin, base API URLs, and admin portal URLs for Ziri mobile app.

## Recent Changes
- Configured default port to `3000` (`http://localhost:3000/api/v1` for iOS/web, `10.0.2.2:3000` for Android emulator) to match Ziri backend server's port.
