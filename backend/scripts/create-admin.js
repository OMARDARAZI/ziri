const bcrypt=require('bcrypt');const {query,pool}=require('../src/config/database');const {normalizePhone}=require('../src/utils/phone');
async function main(){const [fullName,phoneInput,password]=process.argv.slice(2);if(!fullName||!phoneInput||!password){throw new Error('Usage: npm run create-admin -- "Name" +96170000001 Password123!');}const phone=normalizePhone(phoneInput);if(!phone)throw new Error('Invalid phone number');await query(`INSERT INTO users(role,full_name,phone,password_hash,is_active) VALUES('ADMIN',?,?,?,1) ON DUPLICATE KEY UPDATE full_name=VALUES(full_name),password_hash=VALUES(password_hash),is_active=1`,[fullName,phone,await bcrypt.hash(password,12)]);console.log(`Admin ${phone} created or updated.`);await pool.end();}main().catch(error=>{console.error(error.message);process.exitCode=1;});
