<header class="bg-white border-bottom px-3 py-2 d-flex justify-content-between align-items-center"><span class="text-muted small"><%= webUser?.full_name || '' %></span><form method="post" action="<%= webUser?.role === 'ADMIN' ? '/admin/logout' : '/provider/logout' %>"><input type="hidden" name="_csrf" value="<%= csrfToken %>"><button class="btn btn-sm btn-outline-secondary">Sign out</button></form></header>
