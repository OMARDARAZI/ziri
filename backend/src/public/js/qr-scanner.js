document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('#qr-validation-form');if(!form)return;const result=document.querySelector('#scanner-result');const submit=async(value)=>{const button=form.querySelector('button[type=submit]');button.disabled=true;result.innerHTML='<div class="text-muted">Validating…</div>';try{const response=await fetch('/api/v1/provider/qr/validate',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':form.dataset.csrf},body:JSON.stringify({token:value})});const payload=await response.json();result.innerHTML=`<div class="alert alert-${payload.success?'success':'danger'} mb-0">${payload.message}${payload.success?`<hr class="my-2"><strong>${payload.data.participant.full_name}</strong><br>${payload.data.booking.offering_title} · ${payload.data.booking.booking_code}`:''}</div>`;}catch{result.innerHTML='<div class="alert alert-danger mb-0">Could not contact the server.</div>';}finally{button.disabled=false;}};form.addEventListener('submit',(e)=>{e.preventDefault();submit(form.token.value);});if(window.Html5QrcodeScanner){new Html5QrcodeScanner('qr-reader',{fps:10,qrbox:220}).render((value)=>submit(value),()=>{});}});
