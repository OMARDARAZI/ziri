const router=require('express').Router(); const auth=require('../../controllers/admin/auth.controller'); const dashboard=require('../../controllers/admin/dashboard.controller'); const crud=require('../../controllers/admin/crud.controller'); const {login}=require('../../validators/auth.validator'); const {validate}=require('../../middleware/validation.middleware'); const {requireWebRole}=require('../../middleware/web-auth.middleware'); const {csrfProtection}=require('../../middleware/csrf.middleware'); const {imageUpload}=require('../../config/upload'); const {uploadErrors}=require('../../middleware/upload.middleware'); const asyncHandler=require('../../utils/asyncHandler');
router.use(csrfProtection);router.get('/login',auth.loginPage);router.post('/login',login,validate,asyncHandler(auth.login));router.use(requireWebRole('ADMIN'));router.post('/logout',auth.logout);router.get('/dashboard',asyncHandler(dashboard.dashboard)); const upload=(req,res,next)=>{const resource=req.params.resource;if(resource==='providers')return imageUpload('providers').fields([{name:'logo',maxCount:1},{name:'cover_image',maxCount:1}])(req,res,next);const folders={stories:'stories',news:'news',events:'events','safety-tips':'safety-tips',offerings:'offerings'};if(folders[resource])return imageUpload(folders[resource]).single('image')(req,res,next);next();};router.get('/:resource',asyncHandler(crud.list));router.get('/:resource/new',asyncHandler(crud.form));router.get('/:resource/:id/edit',asyncHandler(crud.form));router.post('/:resource',upload,uploadErrors,asyncHandler(crud.save));router.post('/:resource/:id',upload,uploadErrors,asyncHandler(crud.save));router.post('/bookings/:id/action',asyncHandler(crud.bookingAction));router.post('/qr-codes/:id/cancel',asyncHandler(crud.cancelQr));router.post('/:resource/:id/delete',asyncHandler(crud.remove));module.exports=router;
