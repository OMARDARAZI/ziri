const service=require('../../services/booking.service'); const notificationRepo=require('../../repositories/notification.repository'); const {success}=require('../../utils/apiResponse');
async function create(req,res){ const booking=await service.create(req.user,req.body); notificationRepo.createNotification({ userId: req.user.id, title: 'Booking Confirmed 🎉', body: `Your booking #${booking.bookingCode || booking.id} has been successfully placed.`, type: 'BOOKING', referenceId: booking.id }).catch(err => console.error('Notification error:', err)); success(res,booking,'Booking created successfully',201); }
async function list(req,res){ const result=await service.listForCustomer(req.user.id,req.query); success(res,result.items,'Operation completed successfully',200,result.pagination); }
async function get(req,res){ success(res,await service.getForCustomer(req.params.id,req.user.id)); }
async function cancel(req,res){ const booking=await service.cancel(req.params.id,req.user.id); notificationRepo.createNotification({ userId: req.user.id, title: 'Booking Cancelled', body: `Your booking #${booking.bookingCode || booking.id} has been cancelled.`, type: 'BOOKING', referenceId: booking.id }).catch(err => console.error('Notification error:', err)); success(res,booking,'Booking cancelled successfully'); }
module.exports={create,list,get,cancel};
